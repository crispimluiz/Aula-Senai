package com.crispimluiz.senai.interfaceAula;

public interface Interface {
	
	/* todas as classes que implements a essa deverão executar
	 * todos os métodas que estão na interface. Como um contrato.
	 * Cada classe pode SOBREESCREVER o método, mas deverão faze-las.
	 * Nas abstratas vc pode fazer o método, aqui somente a assinatura.
	 */
	void getAcelerar();
	double getSoma(double i, double j);
}
